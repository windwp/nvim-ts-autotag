name: Tests

on: [push, pull_request, workflow_call]

jobs:
  x64-ubuntu:
    name: nix-runner
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: make test
